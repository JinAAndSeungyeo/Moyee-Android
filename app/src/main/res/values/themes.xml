<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Moyee" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:textColor">@color/white</item>
    </style>

    <style name="DefaultTextInputLayout" parent="Widget.Material3.TextInputLayout.FilledBox">
        <item name="boxBackgroundColor">@color/background2</item>
        <item name="boxCornerRadiusBottomEnd">100dp</item>
        <item name="boxCornerRadiusBottomStart">100dp</item>
        <item name="boxCornerRadiusTopEnd">100dp</item>
        <item name="boxCornerRadiusTopStart">100dp</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="boxStrokeColor">@android:color/transparent</item>
        <item name="hintEnabled">false</item>
        <item name="android:layout_marginVertical">@dimen/margin_small</item>
    </style>

    <style name="DefaultTextInputEditText">
        <item name="android:padding">@dimen/padding_small</item>
        <item name="android:textSize">@dimen/text_size5</item>
        <item name="android:maxLines">1</item>
        <item name="android:inputType">text</item>
    </style>

    <style name="CategoryRadioButton" parent="Widget.Material3.CompoundButton.RadioButton">
        <item name="android:textColor">@drawable/selector_category_radio_text_color</item>
        <item name="android:background">@drawable/selector_category_radio</item>
        <item name="android:button">@null</item>
        <item name="android:layout_marginHorizontal">@dimen/margin_small</item>
        <item name="android:paddingHorizontal">@dimen/padding_middle</item>
        <item name="android:textSize">@dimen/text_size5</item>
    </style>
</resources>